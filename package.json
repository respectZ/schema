{
	"name": "schema",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest",
		"prettier": "^3.8.3",
		"prettier-plugin-organize-imports": "^4.3.0",
		"tiny-jsonc": "^1.0.2"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"scripts": {
		"fmt": "prettier --write .",
		"create-schema": "bun scripts/schema.ts",
		"generate": "bun scripts/generator/index.ts",
		"prepack": "bun run generate && bun scripts/prepack.ts"
	}
}
