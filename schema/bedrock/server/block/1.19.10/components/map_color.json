{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/block/1.19.10/components/map_color.json",
	"type": ["string", "array"],
	"title": "Map Color Component",
	"description": "Sets the color of the block when rendered to a map",
	"oneOf": [
		{
			"type": "string",
			"title": "Hex Color",
			"description": "Color represented as a hex value in the format #RRGGBB",
			"pattern": "^#[0-9A-Fa-f]{6}$"
		},
		{
			"type": "array",
			"title": "RGB Array",
			"description": "Color expressed as an array of [R, G, B] from 0 to 255",
			"minItems": 3,
			"maxItems": 3,
			"items": {
				"type": "integer",
				"minimum": 0,
				"maximum": 255
			}
		}
	]
}
