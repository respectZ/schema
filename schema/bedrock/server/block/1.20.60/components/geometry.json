{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/block/1.20.60/components/geometry.json",
	"title": "Geometry Component",
	"description": "The description identifier of the geometry file to use to render this block. This identifier must match an existing geometry identifier in any of the currently loaded resource packs.",
	"anyOf": [
		{
			"description": "A simple geometry identifier string, or one of the vanilla identifiers.",
			"$ref": "../../../../../vanilla/reference/block_geometry.json"
		},
		{
			"type": "object",
			"title": "Geometry Object",
			"description": "An object describing the geometry to use, including bone visibility settings.",
			"additionalProperties": false,
			"required": ["identifier"],
			"properties": {
				"identifier": {
					"title": "Identifier",
					"description": "Specifies the geometry description identifier to use to render this block. This identifier must match an existing geometry identifier in any of the currently loaded resource packs.",
					"$ref": "../../../../../vanilla/reference/block_geometry.json"
				},
				"culling": {
					"title": "Culling",
					"description": "An optional identifer of a culling definition. The culling definition is used to determine which faces of the block should be culled when rendering. The culling definition can be used to optimize rendering performance by reducing the number of faces that need to be rendered. This identifier must match an existing culling definition in any of the currently loaded resource packs.",
					"$ref": "../../../../../vanilla/definition/identifier.json"
				},
				"culling_layer": {
					"$ref": "../../../../../vanilla/definition/identifier.json",
					"title": "Culling Layer",
					"description": "A string that allows culling rule to group multiple blocks together when comparing them.",
					"default": "minecraft:culling_layer.undefined",
					"examples": ["minecraft:culling_layer.default", "minecraft:culling_layer.leaves"]
				},
				"bone_visibility": {
					"title": "Bone Visibility",
					"description": "An optional list of true/false values that define the visibility of individual bones in the geometry file. In order to set up bone_visibility, the geometry file name must be entered as an identifier. After the identifier has been specified, bone_visibility can be defined based on the names of the bones in the specified geometry file on a true/false basis. Note that all bones default to true, so bones should only be defined if they are being set to false. Including bones set to true will work the same as the default.",
					"type": "object",
					"additionalProperties": {
						"oneOf": [
							{
								"$ref": "../../../../../vanilla/definition/molang.json#/$defs/string"
							},
							{
								"$ref": "../../../../../vanilla/definition/molang.json#/$defs/versioned"
							},
							{
								"type": "boolean"
							}
						]
					}
				}
			},
			"allOf": [
				{
					"if": {
						"properties": {
							"identifier": {
								"pattern": "^minecraft:.*$"
							}
						},
						"required": ["identifier"]
					},
					"then": {
						"not": {
							"required": ["bone_visibility"]
						}
					}
				}
			]
		}
	]
}
