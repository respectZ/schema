{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/spawn_rule/1.8.0/delay_filter.json",
	"type": "object",
	"title": "Delay Filter",
	"description": "Delays entity spawning by a configurable amount of time after spawn conditions are met. Useful for preventing immediate respawning after mob deaths or creating spawn cooldowns for rare creatures.",
	"additionalProperties": false,
	"properties": {
		"identifier": {
			"type": "string",
			"description": "The identifier for this delay filter."
		},
		"min": {
			"type": "integer",
			"default": 0,
			"description": "Minimum delay in ticks."
		},
		"max": {
			"type": "integer",
			"default": 0,
			"description": "Maximum delay in ticks."
		},
		"spawn_chance": {
			"type": "integer",
			"default": 100,
			"description": "Spawn chance percentage (0-100)."
		}
	}
}
