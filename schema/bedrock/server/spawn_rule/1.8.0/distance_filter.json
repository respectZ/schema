{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/spawn_rule/1.8.0/distance_filter.json",
	"type": "object",
	"title": "Distance Filter",
	"description": "Controls spawning based on distance from the nearest player. Mobs typically spawn within a certain radius around players (24-128 blocks) and despawn when players move too far away.",
	"additionalProperties": false,
	"properties": {
		"min": {
			"type": "integer",
			"default": 24,
			"description": "Minimum distance in blocks from the nearest player."
		},
		"max": {
			"type": "integer",
			"default": 128,
			"description": "Maximum distance in blocks from the nearest player."
		}
	}
}
