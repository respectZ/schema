{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/biome/1.21.110/components/humidity.json",
	"type": "object",
	"title": "minecraft:humidity",
	"description": "Forces a biome to either always be humid or never humid. Humidity affects the spread chance, and spread rate of fire in the biome.",
	"additionalProperties": false,
	"required": ["is_humid"],
	"properties": {
		"is_humid": {
			"type": "boolean",
			"description": "Whether the biome is forced to be humid."
		}
	}
}
