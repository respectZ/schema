{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/item/1.20.0/components/entity_placer.json",
	"type": "object",
	"title": "Entity Placer",
	"description": "The entity_placer item component specifies the blocks that the item can be placed on.",
	"additionalProperties": false,
	"required": ["entity"],
	"properties": {
		"entity": {
			"$ref": "../../../../../vanilla/server/identifiers.json#/$defs/entity_identifier",
			"title": "Entity",
			"description": "The entity to be placed in the world"
		},
		"use_on": {
			"type": "array",
			"title": "Use On",
			"description": "List of block descriptors that contain blocks that this item can be used on. If left empty, all blocks will be allowed",
			"items": {
				"$ref": "../../../../../vanilla/server/block_descriptor.json"
			}
		},
		"dispense_on": {
			"type": "array",
			"title": "Dispense On",
			"description": "List of block descriptors that contain blocks that this item can be dispensed on. If left empty, all blocks will be allowed",
			"items": {
				"$ref": "../../../../../vanilla/server/block_descriptor.json"
			}
		}
	}
}
