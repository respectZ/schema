{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/components/bribeable.json",
	"type": "object",
	"title": "Bribeable",
	"description": "Defines the way an entity can get into the 'bribed' state.",
	"additionalProperties": false,
	"properties": {
		"bribe_cooldown": {
			"type": "number",
			"default": 2.0,
			"description": "Time in seconds before the Entity can be bribed again."
		},
		"bribe_items": {
			"type": "array",
			"description": "The list of items that can be used to bribe the entity.",
			"items": {
				"$ref": "../../../../../vanilla/server/identifiers.json#/$defs/item_identifier"
			}
		}
	}
}
