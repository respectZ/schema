{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/swell.json",
	"type": "object",
	"title": "Behavior Swell",
	"description": "Allows the creeper to swell up when a player is nearby. It can only be used by Creepers.",
	"additionalProperties": false,
	"properties": {
		"start_distance": {
			"type": "number",
			"default": 10.0,
			"description": "Distance in blocks at which the entity starts to swell."
		},
		"stop_distance": {
			"type": "number",
			"default": 2.0,
			"description": "Distance in blocks at which the entity stops swelling."
		}
	}
}
