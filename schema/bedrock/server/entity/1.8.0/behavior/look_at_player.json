{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/look_at_player.json",
	"type": "object",
	"title": "Behavior Look At Player",
	"description": "Allows the mob to look at the player when the player is nearby.",
	"additionalProperties": false,
	"properties": {
		"angle_of_view_horizontal": {
			"type": "integer",
			"default": 360,
			"description": "Horizontal field of view, in degrees."
		},
		"angle_of_view_vertical": {
			"type": "integer",
			"default": 360,
			"description": "Vertical field of view, in degrees."
		},
		"look_distance": {
			"type": "number",
			"default": 8.0,
			"description": "Maximum distance in blocks at which the entity will look."
		},
		"look_time": {
			"oneOf": [
				{
					"type": "array",
					"items": { "type": "number", "minimum": 0 },
					"minItems": 2,
					"maxItems": 2,
					"description": "Range [min, max] of look time in seconds."
				}
			],
			"description": "Time in seconds the entity will look."
		},
		"probability": {
			"type": "number",
			"default": 0.02,
			"description": "Probability of triggering the look behavior each tick."
		}
	}
}
