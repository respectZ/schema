{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/go_home.json",
	"type": "object",
	"title": "Behavior Go Home",
	"description": "Allows the mob to move back to the position they were spawned.",
	"additionalProperties": false,
	"properties": {
		"goal_radius": {
			"type": "number",
			"default": 0.5,
			"description": "Distance in blocks at which the entity is considered to have reached home."
		},
		"interval": {
			"type": "integer",
			"default": 120,
			"description": "Time in ticks between home checks."
		},
		"on_home": {
			"type": "string",
			"description": "Event to run when the entity arrives home."
		},
		"speed_multiplier": {
			"type": "number",
			"default": 1.0,
			"description": "Movement speed multiplier while going home."
		}
	}
}
