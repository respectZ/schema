{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/random_swim.json",
	"type": "object",
	"title": "Behavior Random Swim",
	"description": "Allows an entity to randomly move through water",
	"additionalProperties": false,
	"properties": {
		"interval": {
			"type": "integer",
			"default": 120,
			"description": "Time in ticks between swim attempts."
		},
		"speed_multiplier": {
			"type": "number",
			"default": 1.0,
			"description": "Movement speed multiplier while swimming."
		},
		"xz_dist": {
			"type": "integer",
			"default": 10,
			"description": "Maximum horizontal distance in blocks of the destination."
		},
		"y_dist": {
			"type": "integer",
			"default": 7,
			"description": "Maximum vertical distance in blocks of the destination."
		}
	}
}
