{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/follow_owner.json",
	"type": "object",
	"title": "Behavior Follow Owner",
	"description": "Allows the mob to follow the player that owns them.",
	"additionalProperties": false,
	"properties": {
		"speed_multiplier": {
			"type": "number",
			"default": 1.0,
			"description": "Movement speed multiplier while following."
		},
		"start_distance": {
			"type": "number",
			"default": 10.0,
			"description": "Distance in blocks at which the entity starts following the owner."
		},
		"stop_distance": {
			"type": "number",
			"default": 2.0,
			"description": "Distance in blocks at which the entity stops following the owner."
		}
	}
}
