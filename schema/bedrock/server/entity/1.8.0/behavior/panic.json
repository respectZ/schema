{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/panic.json",
	"type": "object",
	"title": "Behavior Panic",
	"description": "Allows the mob to enter the panic state, which makes it run around and away from the damage source that made it enter this state.",
	"additionalProperties": false,
	"properties": {
		"force": {
			"type": "boolean",
			"default": false,
			"description": "Whether the entity always panics, regardless of being hurt."
		},
		"prefer_water": {
			"type": "boolean",
			"default": false,
			"description": "Whether the entity prefers to flee into water."
		},
		"speed_multiplier": {
			"type": "number",
			"default": 1.0,
			"description": "Movement speed multiplier while panicking."
		}
	}
}
