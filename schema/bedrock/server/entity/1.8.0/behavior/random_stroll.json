{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/random_stroll.json",
	"type": "object",
	"title": "Behavior Random Stroll",
	"description": "Allows a mob to randomly stroll around.",
	"additionalProperties": false,
	"properties": {
		"interval": {
			"type": "integer",
			"default": 120,
			"description": "Time in ticks between stroll attempts."
		},
		"speed_multiplier": {
			"type": "number",
			"default": 1.0,
			"description": "Movement speed multiplier while strolling."
		},
		"xz_dist": {
			"type": "integer",
			"default": 10,
			"description": "Maximum horizontal distance in blocks of the destination."
		},
		"y_dist": {
			"type": "integer",
			"default": 7,
			"description": "Maximum vertical distance in blocks of the destination."
		}
	}
}
