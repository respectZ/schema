{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/server/entity/1.8.0/behavior/random_fly.json",
	"type": "object",
	"title": "Behavior Random Fly",
	"description": "Allows a mob to randomly fly around.",
	"additionalProperties": false,
	"properties": {
		"can_land_on_trees": {
			"type": "boolean",
			"default": true,
			"description": "Whether the entity may land on top of trees when choosing a destination."
		},
		"xz_dist": {
			"type": "integer",
			"default": 10,
			"description": "Maximum horizontal distance in blocks of the destination."
		},
		"y_dist": {
			"type": "integer",
			"default": 7,
			"description": "Maximum vertical distance in blocks of the destination."
		}
	}
}
