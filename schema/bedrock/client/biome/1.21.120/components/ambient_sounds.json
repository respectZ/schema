{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/client/biome/1.21.120/components/ambient_sounds.json",
	"type": "object",
	"title": "minecraft:ambient_sounds",
	"description": "Set the ambient sounds for the biome. These sounds must be in the 'individual_named_sounds' in a 'sounds.json' file.",
	"additionalProperties": false,
	"properties": {
		"addition": {
			"$ref": "#/$defs/sound_addition",
			"description": "Named sound that occasionally plays at the listener position"
		},
		"loop": {
			"$ref": "../../../../../vanilla/client/identifiers.json#/$defs/individual_named_sound_identifier",
			"description": "Named sound that loops while the listener position is inside the biome"
		},
		"mood": {
			"$ref": "../../../../../vanilla/client/identifiers.json#/$defs/individual_named_sound_identifier",
			"description": "Named sound that rarely plays at a nearby air block position when the light level is low. Biomes without an ambient mood sound will use the 'ambient.cave' sound."
		},
		"underwater_addition": {
			"$ref": "#/$defs/sound_addition",
			"description": "Named sound that occasionally plays at the listener position when underwater"
		},
		"underwater_loop": {
			"$ref": "../../../../../vanilla/client/identifiers.json#/$defs/individual_named_sound_identifier",
			"description": "Named sound that loops while the listener position is inside the biome and underwater"
		},
		"underwater_mood": {
			"$ref": "../../../../../vanilla/client/identifiers.json#/$defs/individual_named_sound_identifier",
			"description": "Named sound that rarely plays at a nearby water block position when the light level is low. Biomes without an underwater ambient mood sound will use the 'ambient.underwater.loop' sound."
		}
	},
	"$defs": {
		"sound_addition": {
			"type": "object",
			"title": "Sound Addition",
			"additionalProperties": false,
			"required": ["asset", "chance"],
			"properties": {
				"asset": {
					"$ref": "../../../../../vanilla/client/identifiers.json#/$defs/individual_named_sound_identifier",
					"description": "Name of the sound asset to play"
				},
				"chance": {
					"type": "number",
					"minimum": 0,
					"maximum": 1,
					"description": "Probability of the sound playing each interval, between 0.0 and 1.0"
				}
			}
		}
	}
}
