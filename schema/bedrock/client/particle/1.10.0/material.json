{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/bedrock/client/particle/1.10.0/material.json",
	"title": "Particle Material",
	"type": "string",
	"anyOf": [
		{
			"const": "particles_opaque",
			"description": "Pixels will be fully opaque, regardless of color."
		},
		{
			"const": "particles_alpha",
			"description": "Pixels with an alpha of 0 will be fully transparent, colored pixels will always be opaque."
		},
		{
			"const": "particles_blend",
			"description": "Enables color blending and transparency in colored pixels, uses a normal blend mode."
		},
		{
			"const": "particles_add",
			"description": "Enables color blending and transparency in colored pixels, uses an additive blend mode. This mode is useful for effects that need to 'glow' in some manner."
		},
		{
			"type": "string"
		}
	]
}
