{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "schema/vanilla/server/filter/entity/filters/owner_distance.json",
	"type": "object",
	"title": "Owner Distance Filter",
	"description": "Tests the distance between the subject and its owner.",
	"properties": {
		"test": true,
		"subject": true,
		"operator": true,
		"value": {
			"type": "number",
			"description": "A floating point value."
		}
	},
	"required": ["value"]
}
